spring:
  main:
    allow-bean-definition-overriding: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test-issuer.com/

auth0:
  audience: https://test-audience.com/api

